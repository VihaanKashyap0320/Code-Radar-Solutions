#include <stdio.h>

int main() {
    char ch; // Declare a variable to store the character
    scanf("%c", &ch); // Read a single character input
    printf("ASCII Value: %d\n", ch); // Print the ASCII value of the character
    return 0;
}