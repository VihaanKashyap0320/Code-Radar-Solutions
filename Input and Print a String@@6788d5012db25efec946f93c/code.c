#include <stdio.h>

int main() {
    char str[1]; // Declare a character array to store the input string
    scanf("%s", str); // Read a single word (string) input
    printf("You entered: %s", str); // Print the entered string
    return 0;
}